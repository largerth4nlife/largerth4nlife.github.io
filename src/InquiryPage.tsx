import {useState} from "react";

export function FeedbackPage(){
  const [sent,setSent]=useState(false); const [name,setName]=useState(""); const [company,setCompany]=useState(""); const [email,setEmail]=useState(""); const [message,setMessage]=useState("");
  function submit(e:React.FormEvent){e.preventDefault();const subject=encodeURIComponent(`Payroll Checker Feedback${company?` — ${company}`:""}`);const body=encodeURIComponent(`Name: ${name}\nCompany: ${company}\nEmail: ${email}\n\nFeedback:\n${message}`);window.location.href=`mailto:skydnr95@gmail.com?subject=${subject}&body=${body}`;setSent(true)}
  return <main className="inquiry-page"><div className="inquiry-hero"><div className="wrap"><small>FEEDBACK</small><h1>Help us improve the Payroll Checker.</h1><p>Give us feedback!</p></div></div><div className="wrap inquiry-content"><section className="inquiry-layout"><div className="inquiry-info"><span className="inquiry-icon">★</span><h2>Your feedback matters</h2><p>Give us feedback!</p><div className="inquiry-contact"><span>Email</span><a href="mailto:skydnr95@gmail.com">skydnr95@gmail.com</a></div><div className="inquiry-list"><div><b>Suggestions</b><span>Ideas for improving the checker</span></div><div><b>Issues</b><span>Report problems or unexpected results</span></div><div><b>Feature requests</b><span>Tell us what you want to see next</span></div></div></div><form className="inquiry-form" onSubmit={submit}><div><label htmlFor="inq-name">Your name</label><input id="inq-name" required value={name} onChange={e=>setName(e.target.value)}/></div><div><label htmlFor="inq-company">Company</label><input id="inq-company" value={company} onChange={e=>setCompany(e.target.value)}/></div><div><label htmlFor="inq-email">Email</label><input id="inq-email" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></div><div><label htmlFor="inq-message">Your feedback</label><textarea id="inq-message" required value={message} onChange={e=>setMessage(e.target.value)} rows={7}/></div>{sent&&<div className="inquiry-sent">Your email client should open with your feedback prepared. If it doesn’t, email <a href="mailto:skydnr95@gmail.com">skydnr95@gmail.com</a> directly.</div>}<button type="submit">Send feedback</button></form></section></div></main>
}
