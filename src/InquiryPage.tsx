import {useState} from "react";

export function InquiryPage(){
  const [sent,setSent]=useState(false); const [name,setName]=useState(""); const [company,setCompany]=useState(""); const [email,setEmail]=useState(""); const [message,setMessage]=useState("");
  function submit(e:React.FormEvent){e.preventDefault();const subject=encodeURIComponent(`Payroll Checker Inquiry${company?` — ${company}`:""}`);const body=encodeURIComponent(`Name: ${name}\nCompany: ${company}\nEmail: ${email}\n\nInquiry:\n${message}`);window.location.href=`mailto:skydnr95@gmail.com?subject=${subject}&body=${body}`;setSent(true)}
  return <main className="inquiry-page"><div className="inquiry-hero"><div className="wrap"><small>SUPPORT & INQUIRIES</small><h1>Let’s talk about your payroll workflow.</h1><p>Need a customized attendance checker, want to request a feature, or have a question about the system? Send us an inquiry and we’ll get back to you.</p></div></div><div className="wrap inquiry-content"><section className="inquiry-layout"><div className="inquiry-info"><span className="inquiry-icon">?</span><h2>Personalized for your company</h2><p>This attendance checker can be customized around your company’s employee schedules, attendance rules, payroll workflow, and reporting requirements.</p><div className="inquiry-contact"><span>Email</span><a href="mailto:skydnr95@gmail.com">skydnr95@gmail.com</a></div><div className="inquiry-list"><div><b>Customization</b><span>Employee schedules, rules, and reports</span></div><div><b>Support</b><span>Questions, issues, and feedback</span></div><div><b>Feature requests</b><span>Ideas for improving the checker</span></div></div></div><form className="inquiry-form" onSubmit={submit}><div><label htmlFor="inq-name">Your name</label><input id="inq-name" required value={name} onChange={e=>setName(e.target.value)} placeholder="Enter your name"/></div><div><label htmlFor="inq-company">Company</label><input id="inq-company" value={company} onChange={e=>setCompany(e.target.value)} placeholder="Company name"/></div><div><label htmlFor="inq-email">Email</label><input id="inq-email" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com"/></div><div><label htmlFor="inq-message">How can we help?</label><textarea id="inq-message" required value={message} onChange={e=>setMessage(e.target.value)} placeholder="Tell us what you need..." rows={7}/></div>{sent&&<div className="inquiry-sent">Your email client should open with your inquiry prepared. If it doesn’t, email <a href="mailto:skydnr95@gmail.com">skydnr95@gmail.com</a> directly.</div>}<button type="submit">Prepare inquiry</button></form></section></div></main>
}
