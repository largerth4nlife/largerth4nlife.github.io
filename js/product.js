document.addEventListener('DOMContentLoaded',()=>{const id=new URLSearchParams(location.search).get('id');const p=PRODUCTS.find(x=>x.id===id)||PRODUCTS[0];document.title=`${p.name} — TapCard`;const el=document.getElementById('product-detail');el.innerHTML=`<div class="product-detail"><div class="detail-art">${App.cardArt(p.style)}</div><div class="detail-copy"><p class="eyebrow">${App.escape(p.tag)}</p><h1>${App.escape(p.name)}</h1><p class="description">${App.escape(p.description)}</p><div class="detail-price">${App.money(p.price)}</div><ul>${p.features.map(f=>`<li>${App.escape(f)}</li>`).join('')}</ul><div class="buy-row"><input class="qty" id="qty" type="number" min="1" value="1" aria-label="Quantity"><button class="button" id="buy">Buy this product</button></div><p class="hint">Buying adds the item to your cart. At checkout, your email app will open with the order details ready to send.</p></div></div>`;document.getElementById('buy').onclick=()=>{const qty=Math.max(1,parseInt(document.getElementById('qty').value,10)||1);Cart.add(p,qty);location.href='checkout.html'};});
